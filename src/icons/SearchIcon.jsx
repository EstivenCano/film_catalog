import { SvgIcon } from "@material-ui/core";
import SearchIcon from "@material-ui/icons/Search";

// eslint-disable-next-line import/no-anonymous-default-export
export default (props) => <SvgIcon {...props} component={SearchIcon} />;
